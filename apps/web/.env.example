NEXT_PUBLIC_API_URL="http://localhost:3333"

# Opcional — monitoramento de erro do lado do navegador (sentry.io, tier
# grátis). Precisa do prefixo NEXT_PUBLIC_ pra entrar no bundle do navegador;
# o do lado do servidor é outra variável, SENTRY_DSN (mesmo valor funciona).
NEXT_PUBLIC_SENTRY_DSN=""
SENTRY_DSN=""
